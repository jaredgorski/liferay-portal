apiVersion: v1
kind: Service
metadata:
  name: {{ include "liferay-cx.fullname" . }}
  labels:
    {{- include "liferay-cx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "liferay-cx.selectorLabels" . | nindent 4 }}
