apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "liferay-cx.fullname" . }}-test-connection"
  labels:
    {{- include "liferay-cx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "liferay-cx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
