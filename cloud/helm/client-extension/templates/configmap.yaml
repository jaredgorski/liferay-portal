apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "liferay-client-extension.ext-provision-configmap.name" . }}
  labels:
    {{- include "liferay-client-extension.ext-provision-configmap.labels" . | nindent 4 }}
  annotations:
    {{- include "liferay-client-extension.ext-provision-configmap.annotations" . | nindent 4 }}
data:
  {{ .Release.Name }}.client-extension-config.json: {{ .Values.clientExtensionConfig.configJson | toJson }}
